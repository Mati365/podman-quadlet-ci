{
  "name": "app-infra",
  "main": "index.ts",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./dist",
    "lint": "npx eslint src/",
    "build": "yarn clean && esbuild ./src/index.ts --bundle --platform=node --target=node20 --outdir=dist --external:../node_modules/*",
    "check:types": "npx tsc --noEmit"
  },
  "devDependencies": {
    "@pulumi/hcloud": "^1.13.0",
    "@pulumi/pulumi": "^3.0.0",
    "@types/node": "^16",
    "esbuild": "^0.18.11"
  }
}
